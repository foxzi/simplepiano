services:
  app:
    build: .
    image: simple-piano-dev
    working_dir: /app
    ports:
      - "127.0.0.1:5174:5173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true

volumes:
  node_modules:
