{
  "name": "simple-piano",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node scripts/test-lessons.mjs && node scripts/test-i18n.mjs && node scripts/test-volume.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:offline": "OFFLINE_BUILD=1 vite build --outDir dist-offline && node scripts/build-offline.mjs"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7"
  }
}
